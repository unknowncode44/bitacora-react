# React + TypeScript + Vite

` 
# Crear proyecto con Vite y TypeScript
npm create vite@latest bitacora-react -- --template react-ts
cd bitacora-react
npm install
# Iniciar servidor para mostrar la velocidad de Vite
npm run dev `

